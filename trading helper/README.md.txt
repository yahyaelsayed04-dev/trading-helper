# AI Trading Helper (Streamlit)

This is a simple Streamlit web app that shows a basic **SMA crossover trading strategy** with stock/forex data from Yahoo Finance.

## 🚀 Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
